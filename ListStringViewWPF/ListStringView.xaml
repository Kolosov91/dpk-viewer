<UserControl x:Class="ListStringViewWPF.ListStringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="150" MinWidth="30" MinHeight="30">
    <Grid>
        <ScrollBar Name="vScrollBar" HorizontalAlignment="Right" Width="20" SmallChange="1" Margin="0,0,0,20" ValueChanged="vScrollBar_ValueChanged" ViewportSize="0.1" />
        <ScrollBar Name="hScrollBar" SmallChange="1" Orientation="Horizontal" Margin="0,0,20,0" Height="20" VerticalAlignment="Bottom" ValueChanged="hScrollBar_ValueChanged" ViewportSize="0.1" />
    </Grid>
</UserControl>
